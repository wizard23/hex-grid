{
  "name": "@asimov/web",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --import tsx --test \"src/**/*.test.ts\"",
    "clean": "node -e \"import('node:fs').then(fs=>{fs.rmSync('dist',{recursive:true,force:true}); fs.rmSync('.vite',{recursive:true,force:true});})\""
  },
  "dependencies": {
    "@asimov/shared": "^0.0.0",
    "preact": "^10.25.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.2",
    "vite": "^6.0.0"
  }
}
